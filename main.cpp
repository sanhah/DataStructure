#include "src/SinglyLinkedList.h"
#include <iostream>

using namespace std;


int main(void) {
    // SinglyLinkedList<double> list(0.1);
    std::cout << "build complete" << std::endl;
    // list.append(0.2);
    // list.printList();
    // list.append(0.3);
    // list.printList();
    // list.append(0.4);
    // list.printList();

    // list.insert(0.0, 0);
    // list.printList();
    // list.insert(2.0, 2);
    // list.printList();
    // list.insert(10.0, 10);
    // list.printList();

    // list.remove(0);
    // list.printList();
    // list.remove(5);
    // list.printList();
    // list.remove(2);
    // list.printList();

    return -1;
}